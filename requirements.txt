# Pinned 2026-07-02 so figure bytes are comparable across machines and runs.
# Bump deliberately (all at once, regenerate all figures in the same commit).
pandas==3.0.3
numpy==2.5.0
scipy==1.18.0
matplotlib==3.11.0
requests==2.34.2
statsmodels==0.14.6
